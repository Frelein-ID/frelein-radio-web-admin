export interface Personalities {
  // Main
  id?: string;
  tracks_id: string;
  personality_id: string;
  // Get value from personality info
  name?: string;
  name_jp?: string;
  nickname?: string;
  image?: string;
  createdAt?: Date;
  updatedAt?: Date;
}
